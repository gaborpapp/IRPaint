env = Environment()

env['APP_TARGET'] = 'IRPaint'
env['APP_SOURCES'] = ['IRPaint.cpp', 'BlobTracker.cpp', 'CaptureParams.cpp',
						'ManualCalibration.cpp', 'PParams.cpp', 'Stroke.cpp',
						'Triangle.cpp', 'Utils.cpp']
env['RESOURCES'] = ['gfx/*', 'shaders/*']
env['DEBUG'] = 0

# OpenCV
env = SConscript('../../../blocks/Cinder-OpenCV/scons/SConscript',
	exports = 'env')

SConscript('../../../scons/SConscript', exports = 'env')
